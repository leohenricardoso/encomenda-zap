export default function Home() {
  return (
    <main style={{ display: "grid", placeItems: "center", minHeight: "100vh" }}>
      <h1>Olá — Next.js (App Router) + TypeScript + ESLint</h1>
    </main>
  );
}
